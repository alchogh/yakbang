# 디자인

이 화면이 **왜** 이렇게 생겼는지를 적는다.

색·간격·폰트의 실제 **값은 [app/globals.css](app/globals.css) 의 `@theme` 하나가 출처다.** 여기에 값을 베껴 적지 않는다. 두 곳에 적으면 반드시 어긋난다.

---

## 이 앱이 주는 인상

**조용한 기록장.** 병원 차트도, 헬스 대시보드도 아니다.

매일 열어서 몇 초 쓰고 닫는 앱이다. 그래서 정보를 빽빽하게 채우는 것보다 **입력 마찰을 줄이는 쪽**이 항상 우선한다. 숫자·그래프·달성률로 사용자를 밀어붙이지 않는다.

"고급스럽다"를 이 앱에서는 이렇게 정의한다:

- 색이 적다 — 화면 대부분이 무채색이고, 강조색은 한 화면에 사실상 한 곳에만 있다.
- 선이 적다 — 카드를 테두리로 가두지 않고 여백으로 나눈다.
- 움직임이 적다 — 애니메이션은 상태가 바뀐 걸 알리는 최소한만.
- 여백이 넉넉하다 — 화면당 담는 정보를 줄여서 여백을 확보한다. 폰트를 줄여서 확보하지 않는다.

장식을 더해서 고급스러워지는 게 아니라, **뺄 수 있는 걸 다 뺀 상태**가 목표다.

## 색

### 무채색이 기본, 강조색은 하나

위계는 **색이 아니라 무게·크기·여백**으로 만든다. 텍스트는 3단계(본문 / 보조 / 비활성)면 충분하다. 부족하다고 느끼면 색을 늘리지 말고 여백을 늘린다.

강조색은 **딥 블루 계열 하나**다. 이 선택에는 이유가 있다:

빨강(위험)·노랑(주의)·초록(안전)은 이미 **판단의 신호**로 읽힌다. 우리는 조합의 안전성에 대해 앱이 판단하지 않기로 했다([CLAUDE.md](CLAUDE.md)). 브랜드 색이 초록이면 기록 화면 전체가 은근히 "괜찮다"고 말하게 된다. 파랑 계열은 그렇게 읽히지 않는 몇 안 되는 색이다.

아이콘([app/icon.svg](app/icon.svg))의 밝은 파랑보다 **채도를 낮추고 톤을 깊게** 간다. 밝은 파랑은 흔하고 가볍다.

### 판단을 색으로 하지 않는다

- 조합·복용량에 대해 **경고색·경고 아이콘을 쓰지 않는다.** ⚠️ 배지, 빨간 테두리, "주의" 라벨 전부 해당한다.
- 사용자가 쓴 글에 담긴 판단은 **그 사람 말로 보이게** 둔다. 앱이 요약해서 색으로 바꾸지 않는다.
- 색을 써야 할 것 같으면 만들지 말고 먼저 묻는다.

### 라이트·다크 둘 다 1급

야간 복용 기록이 흔하다. 다크가 나중에 덧붙이는 테마가 아니다.

다크에서 **완전한 검정을 배경으로 쓰지 않는다.** 순검정 위의 흰 글씨는 눈부시고 번져 보인다. 살짝 든 먹색을 바닥으로 두고, **위로 겹치는 면일수록 밝게** 해서 깊이를 만든다. 다크에서 그림자는 보이지 않으므로 밝기 차이가 유일한 층위 신호다.

## 타이포

### Pretendard

본문의 대부분이 한글이다. 라틴 전용 폰트를 쓰면 한글만 시스템 폴백으로 떨어져서 자간·굵기가 따로 논다. Pretendard는 한글·라틴 굵기가 맞고, 가변 폰트라 파일 하나로 전 굵기를 쓴다.

Google Fonts에 없으므로 **패키지에 든 파일을 직접 서빙**한다. [app/globals.css](app/globals.css) 에서 dynamic subset CSS를 `@import` 하면 `unicode-range` 로 쪼갠 조각 중 화면에 실제로 쓰인 글자가 든 것만 내려받는다 — 전체 variable 파일은 2MB라 통째로 받지 않는다. CDN 링크를 쓰지 않는다 — 외부 요청이 생기고, 폰트 요청만으로도 접속 기록이 남는다. 건강 기록 앱에서 굳이 만들 이유가 없다.

### 규칙

- 굵기는 **셋만** — 본문 / 강조 / 제목. 그 사이를 채우고 싶어지면 굵기가 아니라 크기나 여백 문제다.
- 본문 줄간격은 **넉넉하게** 준다. 한글은 라틴보다 글자 밀도가 높아서 라틴 기준 줄간격이면 답답하다.
- 제목은 **자간을 살짝 좁힌다.** 큰 글자에서 기본 자간은 성기게 보인다. 본문 크기에서는 건드리지 않는다.
- 한글에 **이탤릭을 쓰지 않는다.** 기울임꼴 한글은 폰트가 지원하지 않아 브라우저가 억지로 기울인다.
- 본문 최소 크기는 **16px**. 그 아래로 내려가면 iOS 사파리가 입력 시 화면을 확대한다.
- 숫자(복용량·개수)는 **고정폭 숫자**로. 목록에서 자릿수가 흔들리면 지저분해 보인다.

## 화면

### 모바일이 기준

기준 너비는 **iPhone SE 세로(375px)** 다. 여기서 가로 스크롤이나 잘림이 없으면 통과다. 넓은 화면은 `sm:` 이상으로 확장한다.

- 터치 대상은 최소 **44×44px**. 시각적으로 작아 보여도 터치 영역은 확보한다.
- 세이프 에어리어는 `pt-safe` / `pb-safe` 로 준다([app/globals.css](app/globals.css)). 하단 고정 UI에 `pb-safe` 를 빠뜨리면 홈 인디케이터에 가린다.
- 넓은 화면에서 콘텐츠를 무한정 늘리지 않는다. 최대 폭을 두고 가운데 정렬한다. 한 줄이 길어지면 읽기 어렵다.

### 내비게이션은 하단 탭 셋 — 기록 / 커뮤니티 / 나

앱으로 쓰는 화면이라 엄지가 닿는 아래쪽에 둔다.

**종류(비타민·영양제·약)로 탭을 나누지 않는다.** 하단 탭은 앱의 최상위 영역을 가르는 자리인데 종류는 기록 안의 분류라 층위가 맞지 않고, 세 칸을 종류에 쓰면 커뮤니티가 들어갈 자리가 없어진다. 무엇보다 비타민D·오메가3·유산균처럼 경계가 모호한 것이 많아서, 등록할 때마다 분류를 정하게 만드는 건 입력 마찰을 늘리는 쪽이다.

종류는 **속성으로 달아둔다.** 목록이 길어져 실제로 불편해지면 그때 필터로 꺼낸다. 묶어서 보여줄 일이 생기면 종류보다 **시간대(아침·점심·저녁·자기 전)** 가 먼저다. 사용자가 실제로 움직이는 단위가 그쪽이다.

### 형태

- 모서리 반경은 **두 단계** — 작은 것(버튼·입력) / 큰 것(카드·시트). 세 번째를 만들지 않는다.
- **그림자를 거의 쓰지 않는다.** 면을 나눌 때는 배경 밝기 차이나 머리카락 두께 선을 쓴다. 그림자는 화면 위에 떠 있는 것(모달·시트)에만.
- 카드마다 테두리를 두르지 않는다. 여백으로 나뉘면 선은 필요 없다.

### 움직임

- 짧게(150~200ms), **ease-out**. 사용자 조작에 대한 반응은 즉시 시작해서 부드럽게 멈춰야 한다.
- 위치를 크게 움직이는 것보다 **투명도와 미세한 이동**을 쓴다.
- 로딩 스피너를 기본으로 쓰지 않는다. 들어올 내용의 자리를 미리 잡아둔다.
- `prefers-reduced-motion` 을 존중한다.

## 프라이버시가 보이는 방식

규칙 자체는 [CLAUDE.md](CLAUDE.md) 에 있다. 화면에서는 이렇게 드러난다.

- 공개 여부를 정하는 컨트롤은 **꺼진 상태가 기본**이고, 기본값이 무엇인지 눈에 보여야 한다. 기본이 공개인 토글은 만들지 않는다.
- 기록을 커뮤니티로 보내는 건 **사용자가 매번 선택하는 별도 행동**이다. 기록 저장의 부수 효과가 아니다.
- 빈 화면에서 "기록을 공유해보세요" 같은 유도를 넣지 않는다. 빈 상태는 비어 있어도 된다.
- 무엇을 먹는지는 **스크린샷·알림 미리보기에도 남는다.** 알림 문구에 약 이름을 넣지 않는다.

## 아직 정하지 않은 것

첫 화면을 만들면서 정한다. 미리 정하지 않는다.

- 강조색의 정확한 값 — 지금 `--accent` 는 임시값이다. 실제 화면이 채워지면 확정한다.
- 간격 단계, 컴포넌트 목록 — 같은 패턴이 세 번 반복되면 그때 규칙으로 올린다.
